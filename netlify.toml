[build]
  command = "npm run build"
  publish = "dist"

[context.production.environment]
  NODE_ENV = "production"
  ApiBaseUrl = "https://iperfect-server.onrender.com/api"

[context.deploy-preview.environment]
  NODE_ENV = "development"
  ApiBaseUrl = "https://iperfect-server.onrender.com/api"
